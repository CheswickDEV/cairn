{
  "name": "cairn",
  "version": "1.4.0",
  "description": "AI Context Continuity Engine — warns before the context window degrades, hands off a faithful 7-bucket brief, and keeps a decision/evidence ledger across sessions and models.",
  "author": "CheswickDEV",
  "keywords": ["mcp", "context", "handoff", "compaction"],
  "mcpServers": {
    "cairn": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "CAIRN_DB": "${HOME}/.cairn/cairn.sqlite"
      }
    }
  }
}
